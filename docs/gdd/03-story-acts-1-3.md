# Story Structure: Acts 1-3

**Status:** not_started

The prototype covers exactly three acts in a single room. Each act is a deterministic, designer-authored sequence the player must reproduce by manipulating their past instances. The prototype is complete when the player can finish Act 3.

## Act 1: Wake up

- Clock reads 12:00.
- Two people drag a knocked-out body in through the open North door and place it in the center of the room.
- Fade to black.
- Fade in. Clock reads 5:00.
- The player is standing in the center of the room.
- The North door is dark (spawn-only).
- The South and East doors are lit (enterable).
- The West door is dark.

Door destinations from 5:00:

- South: 12:00 (entering this door triggers the Act 1 cinematic from the player's first-person perspective; the two people knock the player out).
- East: 6:00 (room is empty; only the West door is lit).
- North: dark.
- West: dark at 5:00.

At 6:00, the West door leads back to 5:00.

## Act 2: Meet yourself

The player goes East to 6:00, then West to 5:00. On returning to 5:00, the player sees another instance of themselves (You-1). You-1 repeats what You1 did: walks to the West door and disappears.

Repeat the loop. This time, when You1 returns to 5:00, knock out You-1. Drag the body through the East door to 6:00. Wait for another instance to wake up. That instance (You-2, in the recording) knocks You1 out.

Fade out.

## Act 3: Escape

Fade in. Clock reads 5:00. Repeat the Act 2 sequence to position a knocked-out instance at 6:00. Wait for the other instance to wake.

This time, run toward the West door as the other instance chases you. Both instances get pulled through and arrive at 5:00. Two other instances of the player are now present in the same scene.

Curiosity beat: the two instances are visibly aware of each other. The player teams up with the instance from 6:00 to knock out the 5:00 instance. Drag that instance South to 12:00. Place the body in the center of the room (mirroring Act 1).

Knock out the instance brought from 6:00.

The North door at 12:00 is now open. No one is left to stop the player.

Run through the North door.

**Level complete.**

## Beat dependencies

The acts cannot be executed out of order. Act 3 requires the timeline state produced by Act 2. Act 2 requires the discovery beat from Act 1. The prototype's main job is to make these state transitions legible to the player without text or tutorial.

## Failure recovery

There is no auto-rewind. If the player gets stuck or makes the puzzle unsolvable, hard reset is available in the pause menu. See `17-ui-failure-state.md`.

### Build log

- 2026-05-09: REQ-021 Act 3 mirror integration lands (REQ-021 done; `docs/gdd/40-act-progress-and-narrative-beats.md` section 4 REQ-021 dossier slice). Tests-only slice. New file `tests/sim/act3Mirror.test.ts` adds 2 cases that drive the existing primitives (`InputRecorder`, `wireTraversal`, `TimelineRegistry`, `GhostInstance`, `ActStateObserver`, `applyKnockout`, `applyKnockoutBodyResponse`, `resolveCarryToggle`, `applyCarryPickup`, `applyCarryAttachment`, `applyCarryDrop`, `nextInstanceId`, `mountAct1Cinematic`, `createGhost`, `pointInsideTrigger`) through the GDD's scripted Act 3 mirror beat. Phases 1+2+3+4 reuse the team-up setup from `act3TeamUp.test.ts` via four local helpers (`runLoopOne`, `runLoopTwo`, `beginNewLifetimeAt5`, `runChaseToActive5`): drive Act 2 loops 1+2 to land at active=6 with the player unconscious at 6:00, fade through the implicit Act 2 to Act 3 boundary into a fresh conscious lifetime at 5:00 (observer at `act-3-setup`), walk East to 6:00, file a chasing ghost into bucket 6, drive both bodies through the West portal trigger, traverse the active player back to active=5 (observer at `act-3-chase`), then knock out a 5:00-origin ghost in bucket 5 (observer at `act-3-team-up`). Phase 5 picks up the unconscious 5:00-origin ghost via `resolveCarryToggle` on a one-tick rising edge of pickup; `applyCarryPickup` flips the body to `KinematicPositionBased`. Phase 6 places the player just inside the South trigger, records four input frames with the carry held (`back: true, pickup: true`) and runs `applyCarryAttachment` per tick; `detector.step` then crosses the South trigger at 5:00 (South lit at 5:00 per `DOOR_STATE_BY_HOUR`, routes to 12:00 per `ACT_ONE_PORTAL_SPECS.south.destinationHours`), the lit-portal traversal handler teleports the player to the room center at 12:00, switches active timeline to 12, and the carry state passes through unchanged (`wireTraversal` does not branch on `player.carry` per `ActivePlayerHandle.carry` docstring). Phase 7 drives one carry-attachment tick post-traversal so the body re-anchors to the destination spawn pose (the production path the `dragRegression.test.ts` post-traversal re-anchor case pins) and steps the world; then `applyCarryDrop` flips the body back to `Dynamic`, snaps its translation to `(0, RESTING_Y, 0)`, and the carry state returns to `'idle'`. Phase 8 files a placement-record ghost into bucket 12 via `createGhost` plus `registry.add(12, placementGhost)`: a fresh single-frame `GhostInstance` parked at the room center with `consciousness === 'unconscious'` and `originNormalized = 12/24`, mirroring the `ACT1_KNOCKOUT_BODY_RECORDING` shape from `mountAct1Cinematic`. The original bucket-5 ghost is left untouched so the 5:00 timeline still records what happened there (the team-up's knockout); F-007 partial behavior is the canonical owner of the runtime mechanism that produces the placement-record on a real South-portal traversal of a carried body, and lands in its own follow-up slice. Phase 9 builds the snapshot and asserts `isAct3Mirror(snapshot) === true` plus `observer.update(snapshot)` walks from `act-3-team-up` to `act-3-mirror`. A second case pins the predicate's three conjunctions (currentTimeline === 12; activePlayer.carry.kind === 'idle'; bucket 12 has at least one unconscious ghost within `DROP_CENTER_RADIUS_M = 1.0` of the room origin) as load-bearing by satisfying the full state and dropping each conjunction in turn (currentTimeline switched to 5 via a synthesized snapshot; carry switched to `'carrying'`; the bucket-12 ghost remapped to `'conscious'`; the unconscious ghost's planar position remapped to `(5, 0)` so the distance filter rejects the body) and asserts `isAct3Mirror` returns false on each variant. The synthesized boundary case applies `applyKnockout` AFTER `setActiveTimeline(12)` to avoid the entering-bucket reset (`ghost.reset()` restores consciousness to the seed `'conscious'`, so a pre-switch knockout flag is wiped on the switch). NO production code touched: the existing `isAct3Mirror` predicate from PR #30 fires correctly on the live-state snapshot once the dragged ghost is rehomed into bucket 12, confirming the dossier-pseudocode mapping (`currentTimeline === 12 && carry.kind === 'idle' && ghostsFor(12).some(unconscious && distance <= 1.0)`) is correct end-to-end against live registry / lit-portal / carry / ghost-replay state. Files: `tests/sim/act3Mirror.test.ts`, `docs/gdd/03-story-acts-1-3.md`, `docs/PROGRESS_LOG.md`, `docs/GDD_COVERAGE.json`. PR pending.
- 2026-05-09: REQ-020 Act 3 team-up integration lands (REQ-020 done; `docs/gdd/40-act-progress-and-narrative-beats.md` section 4 REQ-020 dossier slice). Tests-only slice. New file `tests/sim/act3TeamUp.test.ts` adds 2 cases that drive the existing primitives (`InputRecorder`, `wireTraversal`, `TimelineRegistry`, `GhostInstance`, `ActStateObserver`, `applyKnockout`, `applyKnockoutBodyResponse`, `resolveCarryToggle`, `applyCarryPickup`, `applyCarryAttachment`, `nextInstanceId`, `mountAct1Cinematic`, `createGhost`, `pointInsideTrigger`) through the GDD's scripted Act 3 team-up beat. Phases 1+2+3 reuse the chase setup from `act3Chase.test.ts` via four local helpers (`runLoopOne`, `runLoopTwo`, `beginNewLifetimeAt5`, `runChaseToActive5`): drive Act 2 loops 1+2 to land at active=6 with the player unconscious at 6:00, fade through the implicit Act 2 to Act 3 boundary into a fresh conscious lifetime at 5:00 (observer at `act-3-setup`), walk East to 6:00, file a chasing ghost into bucket 6, drive both bodies through the West portal trigger, and traverse the active player back to active=5 (observer at `act-3-chase`). The first three helpers are now used in three test files (`act3Setup.test.ts`, `act3Chase.test.ts`, this slice); a follow-up extraction slice (separate per slice-discipline) will fold them into a shared `tests/sim/_helpers/act2Loops.ts` module. The fourth helper (`runChaseToActive5`) is on its second use here so it remains inline. Phase 4 finds a 5:00-origin ghost in bucket 5 (the loop-1 East-bound ghost-A or the loop-2 dragging-East ghost-C, both with `originNormalized = 5/24` and reset to tick 0 conscious by `setActiveTimeline(5)` when the chase traversal switched the active timeline back to 5) and applies the same side effects the host's punch resolver applies: `applyKnockout(targetGhost.consciousness)` flips it to `'unconscious'`, `applyKnockoutBodyResponse(targetGhost.body, targetGhost.mesh, direction)` writes the impulse plus the mesh tilt. Phase 5 builds the snapshot (with `recentWestEntries: observer.recentWestEntries()` from the chase phase) and asserts `isAct3TeamUp(snapshot) === true` plus `observer.update(snapshot)` walks from `act-3-chase` to `act-3-team-up`. A second case pins the predicate's two conjunctions (currentTimeline === 5; bucket 5 has at least one unconscious ghost whose origin timeline is 5) as load-bearing by satisfying the full state and dropping each conjunction in turn (currentTimeline switched to 6 via a synthesized snapshot; the bucket-5 ghost remapped to `'conscious'` via a projecting `ghostsFor`; the unconscious ghost's `originNormalized` remapped to 6/24 so `timelineIdFromNormalized` returns 6 and the predicate's origin filter rejects the ghost) and asserts `isAct3TeamUp` returns false on each variant. NO production code touched: the existing `isAct3TeamUp` predicate from PR #30 fires correctly on the live-state snapshot, confirming the dossier-pseudocode mapping (`currentTimeline === 5 && ghostsFor(5).some(g => g.consciousness === 'unconscious' && timelineIdFromNormalized(g.originNormalized) === 5)`) is the correct read of REQ-020. Files: `tests/sim/act3TeamUp.test.ts`, `docs/gdd/03-story-acts-1-3.md`, `docs/PROGRESS_LOG.md`, `docs/GDD_COVERAGE.json`. PR pending.
- 2026-05-09: REQ-019 Act 3 chase integration lands (REQ-019 done; `docs/gdd/40-act-progress-and-narrative-beats.md` section 4 REQ-019 dossier slice). Tests-only slice. New file `tests/sim/act3Chase.test.ts` adds 2 cases that drive the existing primitives (`InputRecorder`, `wireTraversal`, `TimelineRegistry`, `GhostInstance`, `ActStateObserver`, `applyKnockout`, `applyKnockoutBodyResponse`, `resolveCarryToggle`, `applyCarryPickup`, `applyCarryAttachment`, `nextInstanceId`, `mountAct1Cinematic`, `createGhost`, `pointInsideTrigger`) through the GDD's scripted Act 3 chase beat. Phase 1 reuses the loop-1 plus loop-2 helpers (`runLoopOne`, `runLoopTwo`, mirrored from `act3Setup.test.ts` per slice-discipline waiting for the third repetition before extracting; this is the second use of the shared shape) to land at active=6 with the player unconscious at 6:00. Phase 2's `beginNewLifetimeAt5` (also mirrored) files the prior unconscious lifetime as a ghost in bucket 6 and opens a fresh conscious lifetime at 5:00, transitioning the observer from `act-2-loop-2` to `act-3-setup`. Phase 3 walks East from 5:00 to traverse to 6:00 (East lit at 5:00 per `DOOR_STATE_BY_HOUR`). Phase 4 authors a chasing ghost via `createGhost` with a 5-frame West-bound recording (the same authoring pattern `mountAct1Cinematic` uses for scripted-actor recordings) and files it into bucket 6. Phase 5 positions both bodies a hair East of the West trigger's East face (player at z=-0.5, chaser at z=0.5; the trigger's `halfZ` is 0.6 so both Z offsets sit comfortably inside the trigger volume, and the 1.0 m Z separation exceeds the player capsule diameter of 0.8 m so the capsules do not overlap and Rapier's contact response does not perturb the trigger entry tick) and drives them West each tick: the chaser via `advanceTick` (recording-driven for 5 frames, then a direct `setLinvel({x:-4, ...})` write so the body keeps crossing under linear damping; the chaser's body has `linearDamping = 8.0` set in `createGhost` so the recording-driven velocity decays after the input frames exhaust), the active player via `setLinvel({x:-4, ...})` mirroring the host's input-to-velocity write. After each `world.step()` both bodies are tested against the West portal trigger via the pure `pointInsideTrigger` predicate; on a false-to-true edge, `observer.recordWestEntry({instanceId, tick})` fires for that instance. The active player's position is also fed to `detector.step(x, z, tick)` so the production lit-portal traversal handler runs (snapshotting the lifetime, teleporting to 5:00, switching active timeline to 5, re-stamping origin tint, advancing instance id). Phase 6 asserts the buffer captured two distinct instance ids within the 2-tick chase window (`Math.abs(activePlayerEntryTick - chaserEntryTick) <= 2`) and the active player's traversal landed at active=5. Phase 7 builds the snapshot with `recentWestEntries: observer.recentWestEntries()` and asserts `isAct3Chase(snapshot) === true` plus the observer transitions from `act-3-setup` to `act-3-chase`. A second case pins the predicate's two conjunctions (currentTimeline === 5; two distinct instance ids in the buffer within CHASE_WINDOW_TICKS) as load-bearing by satisfying the full state and dropping each conjunction in turn (currentTimeline switched to 6 via a synthesized snapshot; two entries with the same id; two distinct ids outside the 2-tick window) and asserts `isAct3Chase` returns false on each variant. NO production code touched: the `recentWestEntries` ring buffer, `recordWestEntry`, `hardReset` clearing of the buffer, the `RECENT_WEST_ENTRIES_CAPACITY` cap, and the `isAct3Chase` predicate all landed with the PR #30 actState observer slice; this test confirms the dossier-pseudocode mapping (`recentWestEntries.distinctInstanceIds(window=2) >= 2 && currentTimeline === 5`) is correct end-to-end against live registry / lit-portal / ghost-replay state. Files: `tests/sim/act3Chase.test.ts`, `docs/gdd/03-story-acts-1-3.md`, `docs/PROGRESS_LOG.md`, `docs/GDD_COVERAGE.json`. PR pending.
- 2026-05-09: REQ-018 Act 3 setup integration lands (REQ-018 done; `docs/gdd/40-act-progress-and-narrative-beats.md` section 4 REQ-018 dossier slice). Tests-only slice. New file `tests/sim/act3Setup.test.ts` adds 2 cases that drive the existing primitives (`InputRecorder`, `wireTraversal`, `TimelineRegistry`, `GhostInstance`, `ActStateObserver`, `applyKnockout`, `applyKnockoutBodyResponse`, `resolveCarryToggle`, `applyCarryPickup`, `applyCarryAttachment`, `nextInstanceId`, `mountAct1Cinematic`, `createGhost`) through the GDD's scripted Act 3 setup beat. Phase 1 reuses the Loop 1 plus Loop 2 sequence (helpers `runLoopOne` and `runLoopTwo`) to land at active=6 with the active player unconscious at 6:00, bucket 5 holding ghost-A unconscious plus the dragging-East ghost-C, bucket 6 holding the loop-1 West-bound ghost-B. The observer is opened mid-test and fed the loop-1 then loop-2 snapshots so the watermark advances to `act-2-loop-2`. Phase 2 begins the Act 3 fresh lifetime at 5:00 via a new `beginNewLifetimeAt5` helper that mirrors the lifetime-rollover the host's traversal handler runs on a lit-portal traversal (`wireTraversal` step 1-7), minus the trigger event (the GDD's Act 2 to Act 3 fade-out / fade-in is the implicit lifetime boundary; there is no portal traversal between the two acts): it files the prior unconscious lifetime as a ghost in bucket 6 (using `lifetime.recorder.snapshot()`, post-flipped to `unconscious` via `applyKnockout` in the same pattern `mountAct1Cinematic` uses for the Act 1 body ghost), advances the active player's `instanceId` via `nextInstanceId` (REQ-007 / REQ-008), opens a fresh `InputRecorder`, restamps `originNormalized` to 5/24 plus `applyInstanceTint`, places the body at the room center with zero velocity / cleared rotation / cleared mesh tilt, then calls `setActiveTimeline(5)` (which hides bucket 6 leaving the unconscious ghost unconscious because hide does NOT reset, and resets bucket 5 ghosts to tick 0 conscious per REQ-033 reset semantics; the act-3-setup predicate does not read bucket 5 so the conscious reset is harmless). To make the recorder snapshot non-empty at the boundary, `runLoopTwo` records one idle frame at 6:00 BEFORE the knockout (the East traversal swapped to a fresh recorder; without the idle frame the recorder would be empty and the prior lifetime would silently skip the file step under the `recording.length > 0` gate `wireTraversal` itself uses). Phase 3 builds the snapshot and asserts `isAct3Setup(snapshot) === true` plus `observer.update(snapshot)` walks from `act-2-loop-2` to `act-3-setup`. The next predicate (`isAct3Chase`) requires two distinct West-portal entries within `CHASE_WINDOW_TICKS`, which is empty in this snapshot, so the walk halts at `act-3-setup`. A second case pins the two conjunctions of `isAct3Setup` (currentTimeline === 5, ghosts6.some(unconscious)) as load-bearing by satisfying the full state then dropping each conjunction in turn (currentTimeline switched to 6 via a synthesized snapshot, no unconscious ghost in bucket 6 by mapping the bucket-6 projection's consciousness to `'conscious'`) and asserting `isAct3Setup` returns false on each variant. NO production code touched: the existing `isAct3Setup` predicate from PR #30 fires correctly on the live-state snapshot once the lifetime-boundary mechanism is exercised, confirming `currentTimeline === 5 && ghostsFor(6).some(unconscious)` is the correct dossier-pseudocode mapping for the Act 3 setup beat (the watermark prerequisite is enforced by the observer's monotonic walk, not by the predicate). Files: `tests/sim/act3Setup.test.ts`, `docs/gdd/03-story-acts-1-3.md`, `docs/PROGRESS_LOG.md`, `docs/GDD_COVERAGE.json`. PR pending.
- 2026-05-09: REQ-017 Act 2 second loop integration lands (REQ-017 done; `docs/gdd/40-act-progress-and-narrative-beats.md` section 4 REQ-017 dossier slice). Tests-only slice. New file `tests/sim/act2Loop2.test.ts` adds 2 cases that drive the existing primitives (`InputRecorder`, `wireTraversal`, `TimelineRegistry`, `GhostInstance`, `ActStateObserver`, `applyKnockout`, `applyKnockoutBodyResponse`, `resolveCarryToggle`, `applyCarryPickup`, `applyCarryAttachment`, `mountAct1Cinematic`) through the GDD's scripted Act 2 second-loop sequence. Phase 1+2 reuses the loop-1 sequence via a `runLoopOne` helper (East at 5:00, West at 6:00, ghost-A in bucket 5 and ghost-B in bucket 6, active=5, ghost-A replayed to exhaustion). Phase 3a positions the active player next to ghost-A and applies the same side effects the host's punch resolver applies: `applyKnockout(ghostA.consciousness)` flips it to `'unconscious'`; `applyKnockoutBodyResponse(ghostA.body, ghostA.mesh, direction)` writes the impulse plus the mesh tilt. Phase 3b drives `resolveCarryToggle` on a one-tick rising edge of pickup and asserts the resulting state is `{ kind: 'carrying', carriedId: ghostA.instanceId }`; `applyCarryPickup` flips ghost-A's body to `KinematicPositionBased`. Phase 3c records four input frames with the carry held (`right: true, pickup: true`) and runs `applyCarryAttachment` per tick; `detector.step` then crosses the East trigger at 5:00 (East lit per `DOOR_STATE_BY_HOUR`), the lit-portal traversal handler files the carrying-East lifetime as a fresh ghost in bucket 5 (so bucket 5 ends with at least 2 ghosts after the East traversal: the original unconscious ghost-A plus the dragging-East ghost-C, both hidden), switches active timeline to 6, and the carry state passes through unchanged (`wireTraversal` does not branch on `player.carry` per `ActivePlayerHandle.carry` docstring). Phase 3d asserts post-traversal: active=6, `player.carry` preserved, bucket 5 has at least one unconscious ghost (ghost-A retained its flag because `setActiveTimeline` only HIDES the leaving bucket; `reset()` only fires on entering-bucket re-entry, not on leaving), bucket 6 still holds ghost-B. Phase 4 resolves the GDD's 'wait for another instance to wake up' beat per Q-023 default A: under REQ-033 done, unconsciousness is permanent in v1 (the dossier explicitly forbids the unconscious-to-conscious transition), so the literal wake mechanic cannot be staged inside one play session; instead the test directly applies `applyKnockout(player.consciousness)` plus `applyKnockoutBodyResponse(player.body, player.mesh, ...)` modelling the outcome of a recorded punch tick from a future loop iteration. The GDD's load-bearing state change is You1 going down at 6:00; the wake mechanic is narrative dressing the recording substrate of REQ-001 / REQ-002 satisfies on the next loop iteration (any future visit to 6:00 will replay this very sequence including the punch). Phase 5 builds the final `ActStateSnapshot` and asserts `isAct2Loop2` returns true plus the observer (advanced to `act-2-loop-1` mid-test via an explicit update at the end of Phase 1+2 so the watermark reflects the chain prerequisite walk) transitions to `act-2-loop-2`. A second case pins the four conjunctions of `isAct2Loop2` as load-bearing: it builds the full satisfying state, then drops each conjunction in turn (currentTimeline switched to 5, active player flipped conscious, no unconscious ghost in bucket 5, empty bucket 6) and asserts `isAct2Loop2` returns false on each variant. NO production code touched: the existing `isAct2Loop2` predicate from PR #30 fires correctly on the live-state snapshot, confirming the predicate's `currentTimeline === 6 && activePlayer.consciousness === 'unconscious' && ghosts5.some(unconscious) && ghosts6.length >= 1` reading is the correct dossier-pseudocode mapping. Files: `tests/sim/act2Loop2.test.ts`, `docs/gdd/03-story-acts-1-3.md`, `docs/PROGRESS_LOG.md`, `docs/GDD_COVERAGE.json`, `docs/OPEN_QUESTIONS.md` (Q-023 added). PR pending.
- 2026-05-09: REQ-016 Act 2 first loop integration lands (REQ-016 done; `docs/gdd/40-act-progress-and-narrative-beats.md` section 4 REQ-016 dossier slice). Tests-only slice. New file `tests/sim/act2Loop1.test.ts` adds 2 cases that drive the existing primitives (`InputRecorder`, `wireTraversal`, `TimelineRegistry`, `GhostInstance`, `ActStateObserver`, `mountAct1Cinematic`) through the scripted Act 2 first-loop sequence: at 5:00 the player records a 5-frame East-bound walk and crosses the East portal trigger (East lit at 5:00 per `DOOR_STATE_BY_HOUR`); the traversal handler files the You1 ghost into bucket 5 (with the East-bound recording, `instanceId = 1`, hidden because timeline 5 is no longer active) and switches the active timeline to 6 with a fresh recorder. At 6:00 the player records a 4-frame West-bound walk and crosses the West portal trigger (West lit at 6:00 per the same table); the traversal handler files the You-1 ghost into bucket 6 (`instanceId = 2`, hidden) and switches the active timeline back to 5, which resets ghost-A to tick 0 and makes it visible. Driving `advanceTick` for ghost-A's full recording length brings `tickIndex >= recordingLength`, satisfying `allGhostsAtRest` in the `isAct2Loop1` predicate. The test then projects the live registry plus the active player into an `ActStateSnapshot` (id, position, consciousness, originNormalized, tickIndex, recordingLength per `BucketGhostSnapshot`) and asserts `ActStateObserver.update(snapshot)` walks the chain through `act-1-spawn` (cinematic ghosts in the 12:00 bucket from `mountAct1Cinematic` at boot) and `act-2-loop-1` (timeline 5, 5:00 ghost at-rest, 6:00 has at least one ghost), halting at the next predicate's first failing condition (`act-2-loop-2` requires timeline 6 with an unconscious active player). A second case pins ghost-A's per-tick replay velocity (`body.linvel().x / .z`) to be byte-identical to `inputToVelocity(recording.frames[i].keys)` for every tick within the recording, backing the GDD's 'and disappears' semantics. NO production code touched: the existing `isAct2Loop1` predicate from PR #30 fires correctly on the snapshot the live state produces, confirming the predicate body's `currentTimeline === 5 && ghostsFor(5).length >= 1 && ghostsFor(6).length >= 1 && allGhostsAtRest(ghostsFor(5))` condition is the correct read of the dossier's pseudocode. Files: `tests/sim/act2Loop1.test.ts`, `docs/gdd/03-story-acts-1-3.md`, `docs/gdd/40-act-progress-and-narrative-beats.md`, `docs/PROGRESS_LOG.md`, `docs/GDD_COVERAGE.json`. PR pending.
- 2026-05-09: Act 1 cinematic at 12:00 lands as scripted actor recordings (REQ-012 done; `docs/gdd/40-act-progress-and-narrative-beats.md` section 5 dossier slice). Two scripted dragger ghosts plus a 1-frame unconscious body ghost are filed into the 12:00 timeline bucket at boot via the new host hook `mountAct1Cinematic({ registry, scene, world })`. New module `src/sim/scripts/act1Cinematic.ts` exports the three frozen `InputRecording` objects (`ACT1_LEFT_DRAGGER_RECORDING`, `ACT1_RIGHT_DRAGGER_RECORDING`, `ACT1_KNOCKOUT_BODY_RECORDING`; the dragger recordings are 240 frames with `back: true, pickup: true` for the first 60 ticks then idle for 180 ticks; the body recording is a single all-zero frame stamped at the 12:00 normalized timeOfDay), the spawn poses (`ACT1_LEFT_DRAGGER_SPAWN = { x: -0.6, z: -3.5 }`, `ACT1_RIGHT_DRAGGER_SPAWN = { x: 0.6, z: -3.5 }`, `ACT1_KNOCKOUT_BODY_SPAWN = { x: 0, z: -3.6 }`), the timing constants (`ACT1_CINEMATIC_DURATION_TICKS = 240`, `ACT1_CINEMATIC_FADE_START_TICK = 180`, `ACT1_CINEMATIC_FADE_DURATION_TICKS = 60`, `ACT1_DRAGGER_WALK_TICKS = 60`), and the host hook itself (which calls `createGhost` three times and `registry.add(12, ghost)` three times, flipping the body ghost to `unconscious` via `applyKnockout` per Q-021 default A). The active timeline at boot is 5:00 so each cinematic ghost is hidden by the registry's `add` non-active-bucket path; they only become visible when the active player visits 12:00 via the South door from 5:00 (which fires `setActiveTimeline(12)` and resets every entering-bucket ghost to its tick-0 spawn pose). New module `src/render/fadeOverlay.ts` exports `createFadeOverlay({ colorHex? })` (Q-013 default A: Three.js full-screen plane on a separate `OrthographicCamera`, `MeshBasicMaterial` with `transparent: true, depthTest: false, depthWrite: false`) plus `setOpacity(t)` (clamped to `[0, 1]`, hides at zero), `render(renderer)` (composites over the main pass with `autoClear = false`), and `dispose()`. `src/app.ts` calls `mountAct1Cinematic(...)` once at boot right after the registry is created and constructs a `fadeOverlay` whose `render` runs after the main `renderer.render` each frame; the overlay opens at opacity 0 (cost-free until a future cinematic-timing slice writes through `setOpacity`). The visible body-tracking-the-actors behavior is OUT of scope this slice: the host loop's carry resolver only fires for the active player, so the body sits at its spawn pose while the actors walk past. A future slice can extend the carry attachment to fire for ghost-to-ghost pickups; this slice ships the data path, the spawn poses, and the fade-overlay primitive. Q-012 / Q-013 / Q-015 / Q-016 / Q-021 defaults are consumed. Files: `src/sim/scripts/act1Cinematic.ts`, `src/render/fadeOverlay.ts`, `src/app.ts`, `tests/sim/scripts/act1Cinematic.test.ts`, `tests/render/fadeOverlay.test.ts`, `docs/gdd/03-story-acts-1-3.md`, `docs/gdd/40-act-progress-and-narrative-beats.md`, `docs/PROGRESS_LOG.md`, `docs/GDD_COVERAGE.json`. PR pending.
- 2026-05-09: ActState observer plus per-beat pure predicates land (REQ-024 partial). The act-progress observer that watches the world for narrative-beat completion is now data-only: `src/sim/actState.ts` exports the `ActState` chain (Act 1 spawn through escaped), every per-beat predicate as a pure function over an `ActStateSnapshot`, the `evaluateActState(snapshot, watermark?)` entry point, and the `ActStateObserver` wrapper that owns the monotonic watermark plus a small recent-West-entries ring buffer for the Act 3 chase predicate. NO host wiring this slice: the per-beat slices (REQ-016 through REQ-023) will wire each predicate to a host signal in turn. The dossier's load-bearing data structure is now in place, so the next slice can wire `act-2-loop-1` straight in. Files: `src/sim/actState.ts`, `tests/sim/actState.test.ts`. PR pending.
- 2026-05-08: hard reset lands end-to-end (REQ-025 done). Pressing `r` returns the simulation to a clean Act 1 spawn pose: every ghost in every timeline bucket is torn down (mesh removed from scene, rigid body removed from world), the active player snaps back to the room center with zero linear velocity, `player.originNormalized` and the capsule tint re-stamp to `ACT_ONE_NORMALIZED`, the active lifetime opens a fresh `InputRecorder` keyed at the Act 1 anchor, the time-of-day clock snaps to 5:00, every door's lit/dark visual repaints to the canonical 5:00 table (South lit, East lit, North dark, West dark), and the portal-trigger overlap state clears so the next step call does not fire a stale exit for whatever trigger the player was standing in at reset. The teardown function is pure (`src/sim/hardReset.ts`); the host (`src/app.ts`) only owns the `KeyR` keydown listener. New helpers: `TimelineRegistry.clearAllGhosts(scene, world, nextActiveTimeline)` walks every bucket, removes meshes from the scene, removes bodies from the world (Rapier's `removeRigidBody` cleans up the body's colliders too), clears each bucket, and resets the active timeline to `nextActiveTimeline`. `PortalTriggerSet.resetOverlapState()` flips every per-portal overlap flag back to `false` without firing events. The function is idempotent (safe to call on a clean state) and correctly tears down ghosts in every visited timeline, not just the active one. Pause-menu UI is OUT OF SCOPE; a single key binding is enough to ship the failure-recovery contract and the next slice's UI can call the same function. Files: `src/sim/hardReset.ts`, `src/sim/timelineRegistry.ts`, `src/sim/portalTrigger.ts`, `src/app.ts`, `tests/sim/hardReset.test.ts`. PR pending.
- 2026-05-08: 6:00 timeline state lands end-to-end (REQ-015 done, REQ-006 done). Traversing East from 5:00 now drops the player into a properly empty 6:00 room with West-only lit and West routing back to 5:00. Added `src/sim/timelineRoom.ts` exposing `repaintDoorsForHour(portals, hour)` and `snapClockToHour(timeOfDay, hour)`. Extended `WireTraversalOptions` with an optional `onTimelineEnter(destinationHour)` callback fired AFTER the registry's active timeline switches; the host wires both helpers into the callback in `src/app.ts`. The traversal handler's lit/dark gate now reads `DOOR_STATE_BY_HOUR[activeTimeline][portal.direction]` for authored hours and falls back to `portal.isLit` for unauthored hours, so the West portal (authored DARK at 5:00 in `ACT_ONE_PORTAL_SPECS`) is enterable at 6:00 per the table without any portal-data rewrite. The 6:00-side data (West-only-lit, West routes to 5:00) was forward-authored in PR #12 and PR #16, so no portal data change was needed for this slice. First entry into 6:00 yields an empty active-ghost list (REQ-006). Files: `src/sim/timelineRoom.ts`, `src/sim/portalTraversal.ts`, `src/app.ts`, `tests/sim/timelineRoom.test.ts`, `tests/sim/portalTraversal.test.ts`. PR pending.
- 2026-05-08: Act 1 spawn pose at 5:00 lands end-to-end (REQ-013 / REQ-014 done). Day-arc convention: a 24-hour cycle on `[0, 1)` with `t = 0.0` at midnight, `t = 1.0` wrapping to midnight, and `hourToNormalized(h) = h / 24`. Picked over a tighter [4:00, 8:00] dawn arc because the GDD calls out 5:00, 6:00, and 12:00 explicitly and the 24-hour mapping lets all three sit on the same scalar that `portalDestinationNormalized` already uses. Added `src/sim/actOneAnchor.ts` (`ACT_ONE_HOUR = 5`, `ACT_ONE_NORMALIZED = 5 / 24`) and `src/sim/doorStateAtTime.ts` (`doorLitStateAtHour(5)` returns South lit / East lit / North dark / West dark per the GDD; `doorLitStateAtHour(6)` is forward-authored as West-only-lit for REQ-015). `src/app.ts` seeds `TimeOfDay` with `initialNormalized: ACT_ONE_NORMALIZED` so the room opens at the 5:00 amber tint and the player capsule's `originNormalized` stamps to 5/24. `src/scene/room.ts` now derives door lit/dark from `doorLitStateAtHour(ACT_ONE_HOUR)` rather than reading `portal.isLit` directly. Files: `src/sim/actOneAnchor.ts`, `src/sim/doorStateAtTime.ts`, `src/scene/room.ts`, `src/app.ts`, `tests/sim/actOneAnchor.test.ts`, `tests/sim/doorStateAtTime.test.ts`, `tests/scene/room.test.ts`. PR pending.
- 2026-05-08: portal traversal teleport wires the Act 1 door destinations (REQ-013 / REQ-014 partial). `wireTraversal` (`src/sim/portalTraversal.ts`) reads each portal's `destinationHours` via `portalDestinationNormalized` and, on a lit entry, teleports the active player to the destination spawn pose, re-stamps `player.originNormalized` to the destination's normalized time, and opens a fresh recorder keyed to that timeline. Combined with the canonical Act 1 portal table from PR #12 (South lit to 12:00, East lit to 6:00, North dark, West dark), entering the South door from 5:00 sends the player to 12:00 and entering the East door sends the player to 6:00 end-to-end. Still partial because the 5:00 clock anchor and per-time spawn poses (the player should physically appear at 12:00 or 6:00 with the room tint at the destination's hour) land in the next slice via the pluggable `resolveSpawnPose` resolver. Files: `src/sim/portalTraversal.ts`, `src/app.ts`, `tests/sim/portalTraversal.test.ts`. PR pending.

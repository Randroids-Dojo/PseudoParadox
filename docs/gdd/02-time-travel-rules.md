# Time Travel Rules

**Status:** partial

The simulation rules that make the game internally consistent. These rules are non-negotiable. If a feature would require breaking one of these rules, the feature is wrong.

## Core rules

1. **Timelines are written by action.** Once a player has performed actions during a time period, those actions are permanent. They will replay as a past instance any time another instance witnesses that period from outside it.
2. **Past instances replay recorded input exactly.** They cannot be changed. They can only be worked around or physically redirected (knocked out, dragged).
3. **Traveling through a portal does not erase the prior timeline.** It adds to it. The player who steps into a portal continues to exist as a recorded instance in the timeline they just left.
4. **No paradoxes.** A player cannot prevent themselves from having done something they already did. The system must enforce this.
5. **Portals are fixed in location and destination.** A given door always leads to the same other time. Destinations do not change once set.
6. **An unvisited future contains nothing.** A time period that no instance has entered has no recorded events and no instances. Timelines spring into existence the first time they are visited.

## Instance numbering

Each time a new instance enters the room, they are one generation further from the original.

- **You1** is always the first-ever spawn (the seed instance, the one the player starts as in Act 1).
- **You-1** is the instance You1 sees arrive (the first replay).
- **You-2** is the instance You-1 sees arrive (the second-order replay).
- The player always controls the most recently spawned active instance.

## Portal types

- **Lit door.** Enterable. Sends the player to the door's fixed destination time.
- **Dark door.** Spawn-only exit. Other instances emerge from these. The player cannot enter them.

A door's lit/dark state is a function of where instances are arriving from in the recorded timeline, not a discoverable variable. The player learns destinations through experience, not through a label.

## Edge cases that must be addressed

- What happens when two instances enter the same portal in the same frame? (See `OPEN_QUESTIONS.md`.)
- What happens when an instance is unconscious as the recording plays back? (Specified in `09-mechanic-instance-replay.md`.)
- What happens if an instance is killed or removed? (Out of scope for v1; killing is not a mechanic. See `99-out-of-scope.md`.)

### Build log

- 2026-05-09: REQ-011 arrivals body deepens for the Act 3 escape (REQ-023 dependency). The `BlockedByArrivals` predicate signature in `src/sim/litStateForTimeline.ts` is extended with a third `timeline?: number` parameter so the rule can scope to a specific (timeline, cardinal) cell, and `DEFAULT_BLOCKED_BY_ARRIVALS` is replaced from the `() => false` MVP stub with the real Acts 1-3 body: at (timeline === 12, cardinal === 'north'), block while `ghosts.some(g => g.tickIndex < g.recording.length)` (the cinematic actors are 'staffing' the door per REQ-023); every other (timeline, cardinal) pair is unblocked. `src/sim/doorStateAtTime.ts`'s `DOOR_STATE_BY_HOUR` adds hour 12 (`{north: true, south: false, east: false, west: false}`) so the seed has a lit cardinal for the arrivals body to darken (the rule is `seed && !blocked`, so a seed-dark cardinal stays dark regardless). The runtime traversal gate in `portalTraversal.ts` already reads through `litStateForTimeline(registry.activeTimeline, { ghosts: registry.ghostsFor(registry.activeTimeline) })`, so the arrivals rule fires unchanged on the existing call site. The `litStateForTimeline` test suite gains six new cases pinning the per-cell rule body. F-006 stays open: the visual paint path (`repaintDoorsForHour`, `room.ts`) still calls `doorLitStateAtHour(12)` directly so the painted North door at 12:00 reads `north: true` unconditionally regardless of whether the cinematic is in flight; the F-006 unification slice routes the paint through `litStateForTimeline` so the painted door darkens during the cinematic too. Files: `src/sim/litStateForTimeline.ts`, `src/sim/doorStateAtTime.ts`, `tests/sim/litStateForTimeline.test.ts`, `tests/sim/doorStateAtTime.test.ts`, `tests/scene/doorVisualLit.test.ts`. PR pending.
- 2026-05-09: REQ-004 done (no-paradox property tests). Tests-only slice; the four invariants from `docs/gdd/40-act-progress-and-narrative-beats.md` section 7 are now codified in `tests/sim/noParadox.test.ts` as property-style tests, each running 100 randomized input sequences derived from a hand-rolled 32-bit Lehmer LCG (Park-Miller multiplier 48271) seeded from the test name via a small djb2 hash (Q-017 default). Invariant 1: `InputRecorder.snapshot()` returns a deeply frozen object whose outer object, frames array, and per-frame keys are all `Object.isFrozen` true; further recording does not perturb the snapshot's observable state; mutation attempts via `frames.push`, `frame.tick = -1`, `frame.keys.forward = true`, and `snap.length = 0` are silently rejected or throw under strict mode and leave the snapshot observably unchanged. Invariant 2: `createTimelineRegistry` keeps the total ghost count across timelines {5, 6, 12} monotonically non-decreasing across a 50-operation random mix of `add` and `setActiveTimeline`; `clearAllGhosts` is the only operation that returns the count to zero. Invariant 3: a ghost built from a recording snapshot at tick K produces bit-identical body translations across 20 fixed simulation steps regardless of whether the source recorder kept recording past K, and `replayAtTick(snap, t)` reads the same value before and after the recorder is mutated past K. Invariant 4: a snapshot's per-frame keys are equality-stable after the recording is filed into a registry and the ghost runs five fixed simulation steps. NO production code touched; the existing `InputRecorder` / `TimelineRegistry` / `GhostInstance` / `replayAtTick` implementations already satisfy every invariant. Files: `tests/sim/noParadox.test.ts`. PR pending.
- 2026-05-09: REQ-005 done (portal destination fixity property test) and REQ-010 done (dark portal exit semantics regression). Tests-only slice. `tests/sim/portal.test.ts` gains a 1000-tick property test that picks a random portal each tick (via a hand-rolled LCG seeded from the test name per Q-017 default) and attempts to assign new values to `destinationHours`, `direction`, and `isLit`; `Object.freeze` rejects every write under strict mode and the per-tick assertions confirm all four portals' fixed fields match their construction-time snapshot. `tests/sim/portalTraversal.test.ts` gains a regression that places the player inside the West dark portal trigger volume at 5:00 (the canonical case from `ACT_ONE_PORTAL_SPECS`), fires a `step` event, and asserts the player's body translation is unchanged, the active timeline is still 5, and no ghost was spawned. The dark-side emergence half of REQ-010 (instances emerging through dark doors) is naturally satisfied by the per-timeline ghost bookkeeping in `src/sim/timelineRegistry.ts`: a ghost recorded entering through a lit portal in its source timeline is filed against that source timeline and replays from its tick-0 spawn pose on re-entry. Files: `tests/sim/portal.test.ts`, `tests/sim/portalTraversal.test.ts`. PR pending.
- 2026-05-08: door lit/dark state derived from arrivals lands (REQ-011 done). The runtime gate that decides whether the active player can enter a portal at the current timeline now reads `seed[timeline][cardinal] && !blockedByArrivals(ghosts, cardinal)` instead of a flat lookup of `DOOR_STATE_BY_HOUR[timeline][cardinal]`. New module `src/sim/litStateForTimeline.ts` exports `litStateForTimeline(timeline, { ghosts, blockedByArrivals? })`, `litStateForCardinal` (single-cardinal sugar), the `BlockedByArrivals` predicate type, and `DEFAULT_BLOCKED_BY_ARRIVALS` (the MVP body that returns `false` for every input). The seed is the canonical Acts 1-3 starting state captured by PR #16 / PR #18 in `DOOR_STATE_BY_HOUR` (5:00 South+East lit; 6:00 West-only-lit). The arrivals predicate is the data path; today's body is a stub returning `false` because Acts 1-3 starting state does not yet have a narrative beat that blocks a door via arrivals (the canonical seeded states already encode the GDD's intent). Future Act 2 / Act 3 slices populate the body. For unauthored hours `litStateForTimeline` returns `null` and `wireTraversal` falls back to the portal's frozen `isLit` flag (preserving pre-REQ-011 behavior for test fixtures that exercise unauthored hours). `src/sim/portalTraversal.ts`'s `isLitForCurrentTimeline` now reads through `litStateForTimeline(registry.activeTimeline, { ghosts: registry.ghostsFor(registry.activeTimeline) })`, threading the registry's per-timeline ghost list into the predicate's input. Reference-equality is preserved on the no-override path (the seed object passes through by reference) so the existing `DOOR_STATE_BY_HOUR is the source-of-truth table` regression keeps holding; on the override path the function returns a fresh object so callers cannot mutate the seed. The visual paint path (`repaintDoorsForHour`, `room.ts`) still calls `doorLitStateAtHour` directly because the arrivals stub returns `false` (paint matches gate); F-006 unifies both call sites once Acts 2-3 introduce a non-trivial arrivals rule. Files: `src/sim/litStateForTimeline.ts`, `src/sim/portalTraversal.ts`, `tests/sim/litStateForTimeline.test.ts` (13 cases). PR pending.
- 2026-05-08: instance generation numbering lands (REQ-007 done, REQ-008 done). `src/sim/instanceId.ts` exports the `InstanceId` type alias (positive monotonic integer; 1 = You1, 2 = You-1, 3 = You-2), `INITIAL_INSTANCE_ID = 1`, `formatInstanceId(id)` (the GDD-canonical display label: `formatInstanceId(1) === 'You1'`, `formatInstanceId(n)` for n > 1 is `'You-' + (n - 1)`), and `nextInstanceId(id)` (returns `id + 1`). All three reject non-integer or non-positive inputs at the boundary so a future call cannot ship a `formatInstanceId(0)` reading as 'You-1'. The id is threaded through `src/scene/player.ts` (`Player.instanceId`, seeded by `createPlayer` at `INITIAL_INSTANCE_ID`), `src/sim/portalTraversal.ts` (`ActiveLifetime.instanceId`, `ActivePlayerHandle.instanceId`), and `src/sim/ghostInstance.ts` (`GhostInstance.instanceId`, `CreateGhostOptions.instanceId`). On every lit-portal traversal `wireTraversal` hands the OUTGOING active player's `instanceId` (via `lifetime.instanceId`) to the spawned ghost (the ghost IS that closed-out instance, replayed) and advances the active player's `instanceId` to `nextInstanceId(previous)`; the freshly-opened lifetime is keyed at the new id (REQ-008). `src/sim/hardReset.ts` resets both `player.instanceId` and `lifetime.instanceId` to `INITIAL_INSTANCE_ID` so the next spawn is again You1. `src/app.ts` populates the initial lifetime with `INITIAL_INSTANCE_ID`. UI rendering of the label is OUT of scope this slice and lands as REQ-032 (thought-bubble overlay). Files: `src/sim/instanceId.ts`, `src/sim/portalTraversal.ts`, `src/sim/ghostInstance.ts`, `src/sim/hardReset.ts`, `src/scene/player.ts`, `src/app.ts`, `tests/sim/instanceId.test.ts` (14 cases), `tests/sim/portalTraversal.test.ts` (5 new cases under "instance generation numbering"), `tests/sim/hardReset.test.ts` (2 new cases under "instance generation reset"), `tests/scene/player.test.ts` (1 new case for the seed). PR pending.
- 2026-05-08: per-timeline ghost bookkeeping lands (REQ-001 done, REQ-003 done, REQ-006 partial). `src/sim/timelineRegistry.ts` exports `createTimelineRegistry({ initialTimeline })`, the `TimelineRegistry` interface (`activeTimeline`, `ghostsFor(timeline)`, `add(timeline, ghost)`, `setActiveTimeline(next)`, `activeGhosts()`), the `TimelineId` type (integer hour), and `timelineIdFromNormalized(normalized)`. Ghosts are now filed against the timeline they were RECORDED IN, not just spawned globally. `wireTraversal` (`src/sim/portalTraversal.ts`) takes a `registry` instead of a flat `ghosts: GhostInstance[]`: each lit-portal entry files the spawned ghost into the SOURCE timeline (derived from `lifetime.originNormalized`) and switches the registry's active timeline to the destination. Filing into a non-active timeline hides the ghost (`mesh.visible = false`) and zeroes its body velocity so it does not coast under residual momentum. `setActiveTimeline(next)` hides every ghost in the leaving bucket and resets every ghost in the entering bucket to its tick-0 spawn pose with zero velocity, then makes them visible. `GhostInstance.reset()` rewinds the tick counter to 0, teleports the body back to the spawn position, zeroes the linear velocity, and resnaps the mesh. The host's per-fixed-step loop in `src/app.ts` now ticks and renders only `registry.activeGhosts()`, so a ghost recorded at 5:00 is hidden while the player is at 6:00 and visible again on return to 5:00. Each timeline visit is a fresh playback (the cleaner mental model: a timeline records WHAT happened, and re-entering replays it from the start). Files: `src/sim/timelineRegistry.ts`, `src/sim/portalTraversal.ts`, `src/sim/ghostInstance.ts`, `src/app.ts`, `tests/sim/timelineRegistry.test.ts` (16 cases), `tests/sim/portalTraversal.test.ts` (4 new cases for per-timeline behavior, 18 existing cases adapted to the registry harness). PR pending.
- 2026-05-08: portal traversal teleport lands (REQ-009 done, REQ-003 / REQ-013 / REQ-014 deepening). `src/sim/portalTraversal.ts` exports `wireTraversal({ detector, player, lifetime, scene, world, ghosts, resolveSpawnPose? })` plus the `ActiveLifetime`, `ActivePlayerHandle`, `TraversalWorldHandle`, `SpawnPoseResolver`, `WireTraversalOptions`, and `TraversalHandle` types. The handler subscribes to the `PortalTriggerSet`'s `enter` events from PR #14, filters on `isLit(portal)` (REQ-010), and on each lit entry: snapshots the active player's lifetime recorder onto a ghost spawned at the lifetime's start position with the lifetime's `originNormalized` as the tint stamp (so the ghost belongs to the timeline being LEFT BEHIND, REQ-001 / REQ-002 / REQ-003), teleports the active player to the destination spawn pose (room center default; pluggable via `resolveSpawnPose` so the next slice can author per-time spawn poses for REQ-013), zeros the player's velocity, re-stamps `player.originNormalized` and the player mesh tint via `applyInstanceTint(destinationNormalized)` (REQ-030), and opens a fresh `InputRecorder` keyed at tick 0 of the destination timeline. Empty recordings (zero frames recorded since the lifetime opened) are not snapshotted into a ghost (no visual noise) but the player is still teleported and the lifetime is reset. `dispose()` unsubscribes from the detector. `src/app.ts` constructs an `ActiveLifetime` next to the player capsule (replacing the standalone `inputRecorder` plus the demo `keydown KeyG` ghost-spawn trigger), wires the traversal handler, and routes per-fixed-step recording into `lifetime.recorder`. Per-timeline ghost bookkeeping (recordings keyed by destination time so a ghost is only visible in the timeline it was recorded in) is OUT OF SCOPE this slice and lands later. Files: `src/sim/portalTraversal.ts`, `src/app.ts`, `tests/sim/portalTraversal.test.ts`. PR pending.
- 2026-05-08: portal trigger overlap detection lands (REQ-009 deepening). `src/sim/portalTrigger.ts` exports `createPortalTrigger(portal)` (builds an XZ-AABB trigger sized 1.2m wide along the wall, 0.6m deep into the room, centered just inside the door's inward face), `pointInsideTrigger(trigger, x, z)` (pure inclusive containment predicate), and `createPortalTriggerSet(portals)` (aggregates four triggers, tracks per-portal overlap state across ticks, emits edge-triggered `OverlapEvent` with `kind: 'enter' | 'exit'` and a tick number, exposes `onPortalOverlap(callback)` returning an unsubscribe function, plus `isOverlapping(portal)` for inspection). Pure-overlap path chosen over a Rapier sensor collider because the room is single-floor and the door footprint is fully known from `DOOR_DIMENSIONS`. The detector emits for both lit and dark portals in this slice; the next slice gates teleport on `isLit(portal)` at the subscriber. `src/scene/room.ts` now returns `{ group, portals }` (a new `RoomBuild` interface) instead of just a Group so the runtime can wire systems against the same Portal instances. `src/scene/scene.ts` carries `portals` through `SceneContext`. `src/app.ts` builds the trigger set, evaluates `step(x, z, tick)` once per fixed simulation step after `world.step()`, and registers a console-only subscriber that logs portal direction, destination hour, lit flag, and tick. Files: `src/sim/portalTrigger.ts`, `src/scene/room.ts`, `src/scene/scene.ts`, `src/app.ts`, `tests/sim/portalTrigger.test.ts`, `tests/scene/room.test.ts`. PR pending.
- 2026-05-08: portal data structure with destination times and stubbed lit/dark lands. `src/sim/portal.ts` exports `Portal` (a frozen object pairing a Door with a `readonly destinationHours` and a stubbed `isLit` flag), `createPortal` (validates finiteness and `[0, 24)` range), `isLit` (pure predicate), `portalDestinationNormalized` (derives `destinationHours / 24` for `TimeOfDay` consumers), `ACT_ONE_PORTAL_SPECS`, and `createActOnePortals(doors)`. The Act 1 canonical table at the 5:00 timeline state lights South to 12:00 and East to 6:00 (REQ-013 / REQ-014) and leaves North and West dark. `src/scene/door.ts` exports `applyDoorLitState(door, isLit)` plus four visual constants; lit doors get a warm diffuse color plus an emissive boost, dark doors get a near-black diffuse and no emissive. `src/scene/room.ts` consumes `createActOnePortals` at build time and stamps the lit/dark visual on each door. Pure data and stub only. Traversal (REQ-009 send-the-player), collision triggers, and timeline-derived lit/dark (REQ-011) are deferred. Files: `src/sim/portal.ts`, `src/scene/door.ts`, `src/scene/room.ts`, `tests/sim/portal.test.ts`, `tests/scene/door.test.ts`, `tests/scene/room.test.ts`. PR pending.
- 2026-05-08: ghost-replay capsule lands. `createGhost({ recording, originNormalized, scene, world, startPosition })` builds a Three.js mesh and a Rapier dynamic capsule sharing the active player's dimensions; its `advanceTick()` writes `replayAtTick(recording, tickIndex)` onto the body's planar velocity each fixed simulation step (preserving y so gravity is not overwritten) and increments an internal tick counter. Past the end of the recording the ghost writes a zero planar velocity and decelerates to a stop under linear damping. The mesh is tinted once at construction with `applyInstanceTint(originNormalized)` so the ghost reads as a different generation than the active player. The same fixed-step loop in `src/app.ts` that already advances physics and `TimeOfDay` now also calls `ghost.advanceTick()` for every active ghost. Pressing `g` in the running app snapshots the current `InputRecorder` and spawns a ghost from the player's spawn position; the recorder keeps recording afterward. Despawn semantics (cleanup of finished ghosts) and portal-driven recording boundaries are deferred to REQ-003. Files: `src/sim/ghostInstance.ts`, `src/app.ts`, `tests/sim/ghostInstance.test.ts`. PR pending.
- 2026-05-08: per-tick input recording buffer lands as the foundation for REQ-001 / REQ-002. Each fixed simulation step pushes the active player's KeyState plus the current TimeOfDay normalized snapshot into an `InputRecorder`. Snapshots are deeply frozen and defensively copied. A pure `replayAtTick(recording, tick)` returns the recorded planar velocity, or a zero vector for ticks past the end of the recording. Files: `src/sim/inputRecorder.ts`, `src/app.ts`, `tests/sim/inputRecorder.test.ts`. PR #7.
